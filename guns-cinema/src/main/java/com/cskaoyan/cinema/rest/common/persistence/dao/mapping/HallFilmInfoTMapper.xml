<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cskaoyan.cinema.rest.common.persistence.dao.HallFilmInfoTMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.cskaoyan.cinema.rest.common.persistence.model.HallFilmInfoT">
        <id column="UUID" property="uuid" />
        <result column="film_id" property="filmId" />
        <result column="film_name" property="filmName" />
        <result column="film_length" property="filmLength" />
        <result column="film_cats" property="filmCats" />
        <result column="film_language" property="filmLanguage" />
        <result column="actors" property="actors" />
        <result column="img_address" property="imgAddress" />
    </resultMap>
    <select id="selectByfieldId" resultType="com.cskaoyan.cinema.vo.cinema.FilmInfoVo">
       select i.actors,i.film_cats as filmCats,i.film_id as filmId,i.film_length as filmLength,
       i.film_name as filmName,i.img_address as imgAddress,
       i.film_language as filmType
       from mtime_hall_film_info_t i
       left JOIN mtime_field_t f on i.film_id=f.film_id
       where f.cinema_id=#{cinemaId} and f.UUID = #{fieldId}
    </select>
</mapper>
